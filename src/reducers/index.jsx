// initialState is stored in the state parameter
// for this project, I'm not changing the value of the project's state at all, so I'll just return whatever content it's initialized with
export default (state) => {
	return state;
}